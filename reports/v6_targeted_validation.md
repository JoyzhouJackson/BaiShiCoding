# V6 定向验证记录

## 验证范围

本轮没有运行12个正式演示算例，只验证当前修改涉及的核心风险：仓容是否真正退出可行域、节点处理能力是否继续生效、成本分项是否能独立复算、滚动快照是否完整且可用于动画。

## 验证结果

1. 4个小型正确性算例 `verify_V01` 至 `verify_V04` 全部求解完成，模型验证、预期业务行为和快照验证均通过。
2. 定向构造一个必须留仓的算例：把节点旧仓容字段压低至0.01吨，货物实际留仓5吨。模型仍完成求解并通过验证，证明旧 `storage_capacity` 字段已经完全退出可行性约束。
3. 每个快照均覆盖全部节点和0至72小时的25个状态时点；节点处理量与留仓量可以由货物行程逐项复算，验证误差为0。
4. 串点班车会在中间节点同时记录车辆分段到达和分段离开，货物行程也保存实际覆盖的每条路段，网页可以还原车辆经过中间节点的动画。
5. Python语法编译和冻结数据完整性校验均通过。

## 异常与处理

曾以60秒上限尝试复测旧版失败算例 `test_urgent_insert_005`，但日初模型在60秒内没有找到可行incumbent，因此未到达原第12小时失败点。该结果仅说明60秒不足以完成这个大算例的日初求解，不能判断V6仓储修改失败。为避免占用正式运行时间，本轮没有继续做300秒完整复测。

正式运行仍采用每次Gurobi调用300秒。新结果写入 `results/gurobi_v6_12`；旧V5结果保留在 `results/gurobi`，不会被续跑逻辑误跳过。
